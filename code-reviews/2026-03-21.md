# Mega Review — 2026-03-21

## Summary

Три из четырёх проектов имеют критические проблемы. Vedic Turkey DOWN (HTTP 404) вторую неделю подряд — CI падает за 10 секунд из-за Prisma без DATABASE_URL в GitHub Secrets. SAMI CI сломан после последнего moderation-коммита. Hunter стабилен (CI зелёный, v0.23.3), Superjob добавлен, но монетизация без deadline отключена неопределённо. Portfolio работает, но GoatCounter мёртв с запуска — счётчик пишет в несуществующий аккаунт. Главные дедлайны: VedicAstroAPI trial (~28 марта, 498 calls), Shopier (ждёт Duygu). Прогресс с прошлой недели: Turkish slugify исправлен, streaming реализован, QA overlay улучшен, Superjob добавлен. Хронически не закрытые: GoatCounter (3 недели), DATABASE_URL CI (2 недели), db.ts split SAMI.

---

## Services Health

| Service | Status | Response | Issues |
|---|---|---|---|
| Vedic Turkey (Vercel) | 🔴 404 | 0.44s | Нет успешного деплоя — CI failing |
| SAMI (Railway) | ✅ OK v0.16.0 | — | uptime 581s, ytDlp available |
| Hunter (Railway) | ✅ OK v0.23.3 | — | — |
| Portfolio (GitHub Pages) | ✅ 200 | 0.59s | GoatCounter не зарегистрирован |
| VedAstro API | ✅ 200 | 1.24s | — |
| Shopier | ⬛ не подключён | — | Ждёт VT-002 (Duygu) |

---

## SAMI

### Code Health

| Severity | Issue | File:line | Fix |
|---|---|---|---|
| critical | CI failing после moderation-коммита (b4c79db) | `.github/workflows/ci.yml` | `npm run typecheck && npm run lint` локально, найти ошибку |
| high | `db.ts` 2399 строк, 184 export — монолит, нечитаемо | `db.ts:1` | split фаза 1: вынести `db-approval.ts` (~250L) с re-export |
| high | `bot-menu.ts` 1903 строки — UGC flow + challenge view зарыты внутри | `bot-menu.ts:1667, 1794` | вынести `sendMyWorkouts`, `sendChallengeView`, `sendFilterResults` в отдельные файлы |
| medium | 63 пустых `catch {}` — ошибки поглощаются молча | `bot-menu.ts`, `downloader.ts:132` | добавить `log.debug/log.warn` |
| medium | `challengeContext` хранится в in-memory Map — теряется при Railway restart | `approval.ts:26` | хранить в DB (добавить колонку в `approval_sessions`) |
| low | `CATEGORY_EMOJI_MAP` дублирует `CATEGORY_EMOJI` из shared.ts | `shared.ts:~213` | удалить дубль, использовать единый объект |

### Tests & Coverage

- Локально: 334 тестов, 0 failed. **CI: FAIL**
- Новое (хорошо): youtube.ts +26 тестов, dates.ts +40 тестов — прогресс
- Не покрыто: `poster.ts` (0 тестов — formatCaption, MarkdownV2 escaping, text fallback при ошибке), `approval.ts` callbacks, `downloader.ts`, `analytics.ts`

### UX & Content

- Сегодня 0 постов, 0 completions. Канал молчит сутки.
- Кардио 6 подряд → 0 completions. Стретчинг → 2/2 completions. Стратег правильно указывает приоритет категорий.
- Sami Score описание избыточное: "(тон, формат, просмотры, лайки, длительность)" → "(качество и популярность)"
- 1/10 знакомых написано лично — 2+ недели без движения

### Backlog

- Open: 43, Done: 282. **Перегружен** — цель ≤15 открытых
- Баланс: ~35% growth, ~35% bugs/debt, ~30% UX
- Stale: личный outreach ("написать 10 знакомым") завис без действий
- Стратег синхронизирован: личный аутрич = единственный реальный рычаг роста при 16 подписчиках

---

## Hunter

### Code Health

| Severity | Issue | File:line | Fix |
|---|---|---|---|
| high | Scorer: 'аналитик' в `irrelevant` убивает score для 'аналитик продукта' и 'ux analyst' | `scorer.ts:281` | добавить 'аналитик продукта', 'product analyst', 'ux analyst' в `ownKeywords` + 2 unit-теста |
| medium | `bot.ts` 950 строк: `handleStats` (~140L) и `handleSubscribe` (~50L) не в handlers/ | `bot.ts:205, 358` | переместить в `handlers/stats.ts`, `handlers/subscribe.ts` |
| medium | Тизеры paywall "мёртвые" во free-phase — при включении монетизации пользователи увидят ограничения без предупреждения | `scheduler.ts:110-118` | написать план мягкого перехода + broadcast за 7 дней до включения |
| medium | Онбординг-гэп до 15 часов: после `sendComplete` бот молчит до следующего scrape | `onboarding.ts:~447` | показать топ-3 кэшированных вакансии сразу после завершения онбординга |
| low | `pdf-parse@1.1.1` — `@ts-expect-error` workaround; latest 2.4.5 устраняет проблему | `resume-import.ts:1` | `npm install pdf-parse@latest` |

### Tests & Coverage

- CI: ✅ зелёный, 10 test files
- Покрыто: основные bot flows, scorer частично, onboarding
- Не покрыто: scrapers (hh, habr, superjob, getmatch), payments flow, cover-letter generation edge cases

### Monetization & UX

- Free phase с 19.03 без критерия выхода — риск дрейфа
- 2 пользователя в DB, 5124 user_vacancies, 8 cover letters сгенерировано — данные накапливаются хорошо
- 4 источника (hh, habr, getmatch, superjob) — отлично. Getmatch игнорирует search queries (API не поддерживает), берёт все IT-вакансии — это норм для IT-специалистов
- AI стек изменён: Gemini 2.5 Pro (cover letters) + Groq Llama 3.3 (resume). **Portfolio не обновлён** — Hunter case всё ещё упоминает "Claude/Haiku"

### Backlog

- Open: 17, Done: 87. Управляемо.
- Критическое: нет плана выхода из free-phase (критерий, дата, broadcast)
- Правильно расставлены приоритеты: scorer fix как P1, plan монетизации как P2

---

## Vedic Turkey

### Code Health

| Severity | Issue | File:line | Fix |
|---|---|---|---|
| critical | CI падает за 10с — Prisma требует `DATABASE_URL` при `prisma generate` | `.github/workflows/ci.yml` | добавить `DATABASE_URL=file:./test.db` в GitHub Secrets |
| critical | Vercel 404 — нет успешного деплоя 2 недели | Vercel dashboard | после фикса CI проверить `npm run build` локально |
| critical | VedicAstroAPI trial: 498 calls, deadline ~28.03 | — | оплатить $18/мес немедленно (VT-703) |
| high | `app/test-admin/page.tsx` — Client Component с `NODE_ENV === 'production'` guard показывает страницу вместо 404 | `test-admin/page.tsx:7` | заменить `return <p>Not available</p>` на `notFound()` из `next/navigation` |
| high | `lib/utils/rateLimit.ts` — in-memory Map сбрасывается при каждом Vercel cold start | `rateLimit.ts:2` | для production использовать Vercel KV или edge config |
| medium | Дублирование: `nakshatraFromLongitude`, `mapSign`, `mapGraha`, `calculateDignity` в двух файлах | `prokerala-chart.ts:98`, `premium-chart.ts:189` | вынести в `lib/astro/utils.ts` |
| medium | `BirthDataForm.tsx` 792 строки — форма + валидация + autocomplete всё вместе | `BirthDataForm.tsx:1` | вынести `useAutocomplete` hook и валидационные функции |
| medium | npm audit: уязвимости с breaking changes | `package.json` | `npm audit` локально, исправить критические |
| low | `app/api/dev/qa-results/route.ts:11` — `writeFileSync` в serverless = бесшумный сбой в продакшне (dev-only, но осторожно) | `route.ts:11` | добавить проверку env или переместить в tmp |

### Calculator & AI

- Калькулятор в BETA, точность не верифицирована (VT-003 открыт, ждёт Duygu)
- Streaming реализован (`generatePremiumReportStream`) — обходит 60s Vercel timeout. Хорошо.
- `middleware.ts` matcher: только `["/"]` — автодетект локали срабатывает только для главной. Прямой вход на `/sade-sati` или `/chart` — локаль не определяется
- Free calculator использует VedAstro, premium использует VedicAstroAPI + Prokerala — архитектура правильная

### Payments & SEO

- Shopier: код готов, ждёт только VT-002
- Forum slugify исправлен (Turkish map) — хорошо
- `og:image` отсутствует на всех страницах (VT-016 в backlog)
- VT-611 (full i18n review) открыт — EN пользователи видят Turkish UI на форуме

### Blockers

- **28 марта:** VedicAstroAPI trial истекает — блокирует premium reports
- **Duygu:** VT-001 (домен), VT-002 (Shopier API keys), VT-003 (chart accuracy), VT-230 (Prokerala на Vercel)

---

## Portfolio

### Code & Design

| Severity | Issue | File:line | Fix |
|---|---|---|---|
| critical | `kombo.html`, `prosto.html`, `skysmart.html`, `teletype.html` содержат `<script src="/qa.js">` — 404 в продакшне | BUG-15 | удалить 4 тега (qa.js уже загружается через `script.js:initQA()` только на localhost) |
| high | GoatCounter URL `diyordesign.goatcounter.com` не зарегистрирован — аналитика мертва с запуска | `index.html:27` | зарегистрировать на goatcounter.com → new site (5 минут) |
| high | `improve/skysmart-enrichment` не смержена — обогащённый Skysmart не в продакшне неделю | git | merge → deploy |
| medium | SYNC-01: Hunter case упоминает "Claude/Haiku", реальный стек — Gemini 2.5 Pro + Groq | `projects/hunter.html:~74,96,128` | обновить RU + EN |
| medium | SYNC-03: SAMI case — устаревший LOC (показывает ~9K вместо 14K+), Seasons→Challenges | `projects/sami.html` | обновить текст в обеих версиях |

### Content & SEO

- index.html: meta, OG, hreflang, structured data — всё на месте. Хорошо.
- GoatCounter код подключён в HTML, но аккаунт не создан → данные не собираются
- About.html не обновлялся — текст шаблонный, ABOUT-02 в бэклоге

---

## Dependencies & Security

| Project | Vulnerabilities | Outdated (критичные) | Action |
|---|---|---|---|
| Hunter | 0 | pdf-parse 1.1.1→2.4.5 | `npm install pdf-parse@latest` |
| SAMI | 0 | @anthropic-ai/sdk 0.78→0.80 | `npm update @anthropic-ai/sdk` |
| Vedic | ⚠️ breaking | next→16.2.1, tailwind→4.2.2, framer→12.38 | `npm audit` локально; prisma 6→7 и next-auth beta→stable — только после анализа breaking changes |
| Portfolio | N/A | — | — |

---

## Cross-Project

**Дублирование:**
- Logger интерфейс: SAMI `createLogger('module')` vs Hunter `logger.info('module', msg)` — разные без причины
- Config zod validation — у обоих ботов одинаковый паттерн. Консистентно, хорошо.
- Rate limiting: Vedic in-memory Map, боты — Railway/grammY защита. Vedic нужен внешний KV в продакшне.

**Несогласованности:**
- SAMI деплоит с автопостингом, Hunter — scheduler с graceful shutdown. Разные подходы к жизненному циклу при Railway restart.
- Только Hunter/SAMI имеют sprint discipline. Vedic backlog (73 open) не привязан к спринтам.

---

## Action Items (готово для бэклога)

1. **Vedic** — critical — Добавить `DATABASE_URL=file:./test.db` в GitHub Secrets → разблокирует CI + Vercel деплой
2. **Vedic** — critical — Оплатить VedicAstroAPI $18/мес до 28 марта — `VT-703`
3. **SAMI** — critical — `npm run typecheck && npm run lint` локально после moderation-изменений, найти и исправить ошибку
4. **Portfolio** — critical — Удалить `<script src="/qa.js">` из `kombo.html`, `prosto.html`, `skysmart.html`, `teletype.html` — BUG-15
5. **Portfolio** — high — Зарегистрировать `diyordesign.goatcounter.com` на goatcounter.com (5 минут)
6. **Portfolio** — high — Смержить `improve/skysmart-enrichment` → main → deploy
7. **Vedic** — high — `app/test-admin/page.tsx:7` — заменить `return <p>Not available</p>` на `notFound()`
8. **Hunter** — high — `scorer.ts:281` — добавить 'аналитик продукта', 'product analyst', 'ux analyst' в ownKeywords + 2 unit-теста
9. **Hunter** — medium — Написать план выхода из free-phase: критерий (50 DAU или 30.04) + broadcast за 7 дней
10. **Hunter** — medium — Immediate digest после онбординга: топ-3 кэшированных вакансии — `onboarding.ts:~447`
11. **SAMI** — medium — db.ts split фаза 1: вынести `db-approval.ts` (~250L) с re-export — `db.ts`
12. **SAMI** — medium — Тесты `poster.ts`: formatCaption, MarkdownV2 escaping, text fallback при ошибке скачивания
13. **Vedic** — medium — `npm audit` + patch обновления: `next@16.2.1`, `framer-motion@12.38`, `tailwindcss@4.2.2`
14. **Portfolio** — medium — SYNC-01: Hunter case — "Claude/Haiku" → "Gemini 2.5 Pro + Groq Llama 3.3" — `projects/hunter.html:~74,96,128` + EN
15. **Portfolio** — medium — SYNC-03: SAMI case — LOC (14K+), Seasons→Challenges — `projects/sami.html`
16. **Vedic** — medium — Вынести `nakshatraFromLongitude`, `mapSign`, `calculateDignity` в `lib/astro/utils.ts` — дублирование в `prokerala-chart.ts:98` и `premium-chart.ts:189`
