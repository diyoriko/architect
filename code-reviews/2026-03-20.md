# Mega Review — 2026-03-20

## Summary

Vedic Turkey вернулся в статус DOWN: Vercel отдаёт 404 вместо сайта. Эта проблема — вероятнее всего следствие незакрытых ESLint-ошибок из прошлого ревью, которые блокируют сборку. Прогресс с прошлой недели: ценообразование исправлено, Claude для premium подключён, streaming реализован. Но три новых критических: сайт недоступен, admin-key захардкожен в публичном репо, VedicAstroAPI trial истекает через 11 дней. Hunter и SAMI стабильны технически, но Hunter по-прежнему не монетизируется (0 платящих). Portfolio слепое — GoatCounter не работает с момента запуска.

## Services Health

| Service | Status | Response | Issues |
|---|---|---|---|
| Vedic Turkiye (Vercel) | 🔴 404 | 0.82s | Сайт недоступен для пользователей |
| SAMI (Railway) | ✅ OK v0.16.0 | — | uptime 18466s |
| Hunter (Railway) | ✅ OK v0.22.0 | — | — |
| Portfolio (GitHub Pages) | ✅ 200 | 0.73s | GoatCounter мёртв |
| VedAstro API | ✅ 200 | 1.38s | — |
| Shopier | ⬛ не подключён | — | Ожидает VT-002 (Duygu) |
| GoatCounter | 🔴 мёртв | — | diyordesign.goatcounter.com не зарегистрирован |

---

## SAMI

### Code Health

| Severity | Issue | File:line | Fix |
|---|---|---|---|
| critical | `JSON.parse(payload.packet)` — crash если packet невалиден, **3-я неделя открыт** | `index.ts:~530` | try/catch + fallback |
| high | `challengeContext` хранится в in-memory Map, теряется при рестарте Railway | `approval.ts:26` | сохранить в DB (колонка в approval_sessions) |
| medium | `body += chunk` строкова конкатенация O(n²) для HTTP body | `index.ts:382-383` | `const chunks=[]; chunks.push(chunk); Buffer.concat(chunks)` |
| medium | `db.ts` — 2399 строк, 161 export. Монолит. При росте — нечитаем | `db.ts` | split на db-members.ts, db-challenges.ts, db-approval.ts |
| medium | `bot-menu.ts` — 1903 строки, всё в одном файле | `bot-menu.ts` | вынести UGC flow (~400 строк) и challenge view (~300 строк) |
| low | Дублирующийся `CATEGORY_EMOJI_MAP`, унифицировать с `CATEGORY_EMOJI` | `shared.ts:~213` | удалить дубль, везде использовать `CATEGORY_EMOJI` |
| low | Множественные `catch {}` без логирования | `downloader.ts:132`, `bot-menu.ts` | добавить `log.warn` |

### Tests & Coverage

- Passed: 268, Failed: 0
- Хорошо покрыто: scheduler, moderation, db-core, e2e flows
- **Критические пробелы:** `youtube.ts` — 0 тестов (searchYouTube, scoring), `poster.ts` — 0 тестов (formatCaption, MarkdownV2 escaping), `dates.ts` — 0 тестов, `approval.ts` callbacks — 0 тестов

### UX & Content

- Автопостинг работает (2 поста сегодня после 4 дней тишины)
- Кнопка "Опубликовать сегодня" двусмысленна — нужно "Опубликовать сейчас" (`bot-menu.ts`)
- 16 подписчиков, **2 выполнения за всё время** — UX механики (стрик, "Я сделаль") не верифицированы живым пользователем

### Backlog

- Open: 43, Done: 258
- Баланс: ~30% growth, ~40% bugs/debt, ~30% features
- Критический незакрытый баг (JSON.parse) висит 3 недели — нужен P0 в текущий спринт
- **Stale:** задачи "Монетизация: опрос в группе" и "Тип Челлендж — DB-схема" помечены как "отложено до 50+ участников" — корректно
- Стратег корректно выделяет личный аутрич как главный приоритет, бэклог синхронизирован

---

## Hunter

### Code Health

| Severity | Issue | File:line | Fix |
|---|---|---|---|
| high | CLAUDE.md устарел: написано "Anthropic SDK, claude-sonnet-4" — код использует Gemini/Groq | `CLAUDE.md` | обновить секцию AI |
| medium | `@anthropic-ai/sdk` установлен, нигде не импортируется в prod-коде | `package.json` | удалить зависимость |
| medium | `config.ts` объявляет `ANTHROPIC_API_KEY` как optional, но он нигде не используется | `config.ts:8` | убрать из schema |
| medium | `ai-client.ts` — нет retry при rate limit от Gemini (429) | `ai-client.ts:46` | добавить exponential backoff |
| low | `@anthropic-ai/sdk` 0.78.0, latest 0.80.0 — если зависимость остаётся | `package.json` | update или удалить |

### Tests & Coverage

- Passed: 430, Failed: 0
- Хорошо покрыто: scheduler, push notifications, scoring
- Не покрыто: `ai-client.ts` (Gemini/Groq fallback logic), `resume-import.ts` validation

### Monetization & UX

- **0 платящих пользователей** — главная проблема проекта
- Конверсионная воронка не видна: нет метрик onboarding completion → trial → paywall hit → payment
- `/adminstats` есть, но не ясно что именно считается
- Trial reminder за 3 дня есть, **за 1 день — нет** (в бэклоге P0)
- Freemium flow корректен по коду, но без данных нельзя понять где отваливаются

### Backlog

- Open: 15, Done: 71
- Баланс: ~60% growth/features, ~20% quality, ~20% tech
- Правильный фокус на Superjob + Careerspace scrapers
- Задача "Конверсионный аудит" — P0 и она правильно первая в списке

---

## Vedic Türkiye

### Code Health

| Severity | Issue | File:line | Fix |
|---|---|---|---|
| critical | **Сайт 404 на Vercel** — скорее всего ESLint провалил build (из прошлого ревью не закрыты 6 ошибок) | `src/app/reports/page.tsx:289` и др. | исправить ESLint ошибки: `<a>→<Link>`, prefer-const, no-explicit-any, unused import |
| critical | Admin key захардкожен: `process.env.ADMIN_SECRET \|\| "1406"` — любой может генерировать premium reports бесплатно | `app/api/reports/generate/route.ts:25` | убрать fallback, требовать только env var |
| high | npm audit показывает уязвимости, требует `--force` для fix | `package.json` | запустить `npm audit` и идентифицировать пакеты |
| high | VedicAstroAPI trial истекает ~Mar 31 2026, 498 вызовов осталось | `CLAUDE.md`, VT-703 | оплатить $18/мес до истечения |
| high | Shopier не подключён — кнопка "Satın Al" ведёт на `/iletisim` вместо реального payment flow | `app/reports/page.tsx:189`, `app/en/reports/page.tsx` | VT-701: реализовать после VT-002 |
| medium | Используется `claude-sonnet-4-20250514` — это Claude Sonnet 4, не 4.6. Следует использовать `claude-sonnet-4-6` | `lib/ai/interpret.ts:52, 77` | обновить model ID |
| medium | `ContactForm.tsx` — catch блок `setStatus("success")` без логирования, ошибки невидимы | `app/iletisim/ContactForm.tsx:87-89` | добавить `console.error` и опционально показать ошибку |
| medium | `prokerala-chart.ts` 515 строк, `calculate.ts` 572 строки — сложные, без тестов | `lib/astro/prokerala-chart.ts`, `lib/jyotish/calculate.ts` | добавить integration tests (VT-504) |
| low | `BirthDataForm.tsx` 774 строки — можно вынести validation logic | `components/chart/BirthDataForm.tsx` | low priority |

### Calculator & AI

- Калькулятор в BETA — Duygu не завершила верификацию 10 чартов (VT-003 открыт)
- 3 API дают идентичные позиции (верифицировано 19.03) — хороший результат
- Streaming реализован и работает — прогресс по VT-700
- Premium reports используют Claude — соответствует CLAUDE.md
- **Тесты только 13** — все unit-уровня (промпты, типы), нет API route тестов

### Payments & SEO

- Shopier: нет flow, `SHOPIER_API_KEY/SECRET` не добавлены в Vercel (ждут Duygu)
- Кнопки покупки ведут на `/iletisim` — пользователь не может купить
- SEO: мета-теги есть, `hreflang` tr/en настроен, Plausible подключён
- Email gate в ChartResult работает (Brevo интеграция)

### Blockers

- **Duygu:** VT-001 (домен), VT-002 (Shopier API), VT-003 (верификация чартов), VT-005 (visual direction)
- **Срочно:** VT-703 (VedicAstroAPI), исправить 404 (no блокер со стороны Duygu)

---

## Portfolio

### Code & Design

| Severity | Issue | File:line | Fix |
|---|---|---|---|
| high | **GoatCounter мёртв** — ноль аналитики с момента запуска. `diyordesign.goatcounter.com` не зарегистрирован | `index.html:32` | зарегистрировать slug на goatcounter.com |
| medium | Hunter case: 3 строки про AI неверны (Claude Haiku → Groq Llama 3.3) | `projects/hunter.html:~74,96,128` + EN версия | SYNC-01: обновить 6 строк |
| medium | Vedic case: описывает тёмную космическую тему — актуальная Flora pink тема | `projects/vedic.html` + EN | SYNC-02: обновить секцию визуального языка |
| medium | SAMI case: "Seasons" вместо "Challenges", LOC 9K→14K | `projects/sami.html` + EN | SYNC-03: rename + metrics |
| low | Дублирующийся `.project-list` в CSS | `styles.css` | объединить |
| low | Mobile menu: `display:none→flex` без анимации | `styles.css` | `visibility`+`opacity` |

### Content & SEO

- 5 избранных кейсов живут, Skysmart детальный
- SAMI/Hunter кейсы устарели: нет визуалов, AI провайдеры изменились
- Osme — флагманский кейс, **0% готовности** при 105 открытых задачах
- LinkedIn Featured не добавлен, портфолио не анонсировано нигде

---

## Dependencies & Security

| Project | Vulnerabilities | Outdated | Action needed |
|---|---|---|---|
| Hunter | 0 | @anthropic-ai/sdk 0.78→0.80, @types/node 20→25, pdf-parse 1.1.1→2.4.5 | удалить @anthropic-ai/sdk; pdf-parse major update — проверить breaking changes |
| SAMI | 0 | @anthropic-ai/sdk 0.78→0.80, @types/node 20→25 | minor; @anthropic-ai/sdk актуален для стратега |
| Vedic | **есть уязвимости** (--force required) | framer-motion 12.36→12.38, next 16.1.6→16.2.0, react 19.2.3→19.2.4, tailwindcss 4.2.1→4.2.2 | **запустить npm audit; patch tailwind/next в первую очередь** |
| Portfolio | n/a (vanilla) | — | — |

---

## Cross-Project

**Дублирование:**
- Logger: SAMI (`logger.ts` 53 строки) и Hunter (`logger.ts` 35 строк) — похожие но разные реализации. Не критично пока проекты независимы.
- Config-via-zod паттерн идентичен в обоих ботах — хорошая консистентность.

**Несогласованности:**
- Hunter переехал на Gemini/Groq, SAMI использует Anthropic SDK для стратега — осознанное разделение, OK.
- Hunter CLAUDE.md устарел (3 дня после AI switch), SAMI CLAUDE.md актуален — нужно синхронизировать Hunter документацию.

---

## Action Items

### Немедленно (блокируют)
1. **Vedic** — critical — исправить ESLint ошибки (no-html-link-for-pages, prefer-const, no-explicit-any, no-unused-vars) → разблокировать Vercel build и вернуть сайт — `src/app/reports/page.tsx:289`, `src/lib/pdf/generateReport.ts:84`, `tests/chart-verification.ts:99,142,143,148`, `src/app/api/sade-sati/route.ts:177`
2. **Vedic** — critical — убрать fallback `"1406"` в admin key, требовать только env var — `app/api/reports/generate/route.ts:25`
3. **Vedic** — high — запустить `npm audit`, идентифицировать уязвимые пакеты, пропатчить — `package.json`
4. **Vedic** — high — оплатить VedicAstroAPI $18/мес до Mar 31 (498 вызовов осталось, истекает) — VT-703

### Sprint-приоритет
5. **SAMI** — critical — исправить `JSON.parse(payload.packet)` в `/packet` handler (3-я неделя открыт) — `index.ts:~530`
6. **SAMI** — high — сохранить `challengeContext` в DB вместо in-memory Map — `approval.ts:26`
7. **Hunter** — high — обновить CLAUDE.md: секция AI (Gemini/Groq), удалить упоминание Anthropic SDK — `CLAUDE.md`
8. **Hunter** — high — запустить `/adminstats`, добавить метрики onboarding completion rate и trial→paid conversion — `handlers/admin.ts`
9. **Portfolio** — high — зарегистрировать `diyordesign.goatcounter.com` на goatcounter.com — `index.html:32`

### Следующие 7 дней
10. **Hunter** — medium — удалить `@anthropic-ai/sdk` из зависимостей (не используется) — `package.json`
11. **Hunter** — medium — убрать `ANTHROPIC_API_KEY` из zod config schema — `config.ts:8`
12. **Vedic** — medium — обновить Claude model ID: `claude-sonnet-4-20250514` → `claude-sonnet-4-6` — `lib/ai/interpret.ts:52,77`
13. **SAMI** — medium — заменить `body += chunk` на `Buffer.concat(chunks)` — `index.ts:382-383`
14. **Portfolio** — medium — обновить Hunter case (SYNC-01): 6 строк про AI provider (RU + EN) — `projects/hunter.html:~74,96,128`
15. **Portfolio** — medium — обновить Vedic case (SYNC-02): секция визуального языка → Flora pink тема — `projects/vedic.html`
16. **Portfolio** — medium — обновить SAMI case (SYNC-03): Seasons→Challenges, LOC 14K+ — `projects/sami.html`
17. **SAMI** — low — удалить `CATEGORY_EMOJI_MAP`, везде использовать `CATEGORY_EMOJI` — `shared.ts:~213`
