# Mega Review — 2026-03-23

## Summary

Vedic Turkey остаётся DOWN (HTTP 404) третью неделю подряд — это критический регресс, сайт недоступен в продакшн. VedicAstroAPI trial истекает через ~5 дней (28 марта, 498 вызовов) — прямой риск сломать premium отчёты. Hunter технически в лучшей форме за всё время (v0.24.2, 5 источников, scorer overhaul) но стоит: 0 пользователей, 7 дней без дистрибуции. SAMI прогресс хороший — Sprint 9 закрыт, db-approval.ts вынесен, 387 тестов — но 0 completions и 0 постов сегодня. Portfolio работает, GoatCounter мёртв третью неделю. Главный паттерн: технический долг закрывается, дистрибуция стоит у всех четырёх проектов.

---

## Services Health

| Service | Status | Response | Issues |
|---|---|---|---|
| Vedic Turkey (Vercel) | 🔴 404 | 1.03s | 3-я неделя 404, деплой сломан |
| SAMI (Railway) | ✅ OK v0.16.0 | 123s uptime | ytDlp OK, последний пост сегодня 06:19 |
| Hunter (Railway) | ✅ OK v0.24.2 | — | стабилен |
| Portfolio (GitHub Pages) | ✅ 200 | 1.03s | GoatCounter не зарегистрирован |
| VedAstro API | ✅ 200 | 1.61s | — |

---

## SAMI

### Code Health

| Severity | Issue | File:line | Fix |
|---|---|---|---|
| high | `db.ts` 2310 строк, 171 экспорт — db-approval.ts вынесен (прогресс!), остальные 4 фазы висят | `db.ts:700-2310` | SM-1010: вынести videos, posts, members, challenges (~1800L) |
| high | `bot-menu.ts` 1984 строки — sendMyWorkouts (1745), sendChallengeView (1875), sendFilterResults (1950) зарыты внутри | `bot-menu.ts:1745,1875,1950` | Вынести 3 функции в `bot-workouts.ts`, `bot-challenge.ts` |
| medium | `moderation.ts` 971 строк — sendFirstCompletionDM (32), celebrate (100), registerModeration (316) — три несвязанных домена | `moderation.ts:1` | Вынести completions в `completion-dm.ts` |
| medium | `challengeContext` хранится in-memory в approval.ts — теряется при Railway restart | `approval.ts` | Уже есть `challenge_context` в DB schema (db-approval.ts:75) — убедиться что используется |
| medium | `strategist.ts` 698 строк — смешаны DB-миграции, AI-промпты, callback-хендлеры | `strategist.ts:221,365,401` | Вынести buildPrompt + extractPacket |
| low | Нет инициализации навигации мобильного меню в коде сайта | — | Уточнить, работает ли бургер на diyor.design |

### Tests & Coverage

- Тестов: 387 (18 файлов) — прогресс с 334
- Проходят локально: ✅. CI статус недоступен
- Не покрыто: `poster.ts` (0 тестов), `approval.ts` callbacks, `downloader.ts`, `analytics.ts`
- Хорошо: youtube.ts, dates.ts — полное покрытие добавлено в Sprint 9

### UX & Content

- 0 completions today, 0 posts today (пост запланирован на 2026-03-24 stretching)
- Всего 3 completions за всё время при 11 постах — 27% engagement, слишком низко
- 16 подписчиков, delta=0 — рост стоит уже несколько дней
- SM-1000 (написать 10 знакомым): 1/10 за 2+ недели — реальный блокер роста
- Retention: вчера 1 активный, сегодня 0 вернулись — первые признаки оттока

### Backlog

- Open: 13, Done: 313. Баланс: ~40% growth (ручная работа), ~30% architecture, ~30% отложено
- Стратег: SM-1000–SM-1003 (аутрич, trackable links, welcome DM) — правильный фокус
- Блокеры: VC.ru статья заблокирована порогом "после 20+ подписчиков" — нужно 4 подписчика

---

## Hunter

### Code Health

| Severity | Issue | File:line | Fix |
|---|---|---|---|
| high | `resume-import.ts`: `new PDFParse({ data: buffer })` — API pdf-parse v2 нестандартный, может падать молча | `resume-import.ts:22-24` | Проверить в REPL: `const { default: pdfParse } = await import('pdf-parse'); pdfParse(buffer)` |
| high | `bot.ts` 926 строк — `handleDigest` (61), `handleProfile` (126), `handleStats` (174) с 137 строками кода | `bot.ts:174-311` | Вынести handleStats в `handlers/stats.ts` |
| medium | `onboarding.ts` 964 строки — 16 состояний + editingSessions Map + buildStepKeyboard все в одном файле | `onboarding.ts:167,377,564` | Вынести `editingSessions` + `handleEditingTextInput` в `onboarding-edit.ts` |
| medium | Дублирующий JSDoc на cover-letter.ts:107 (`/** Generate cover letter via free AI...*/` написан дважды) | `cover-letter.ts:107-108` | Удалить дубль |
| medium | `scheduler.ts` 341 строк — 7 независимых cron-функций в одном файле | `scheduler.ts:74,130,159,196,243,275,309` | Пока OK, приоритет низкий |
| low | `config.ts` deployNotes захардкожены — при каждом деплое надо помнить обновить | `config.ts:25-33` | Добавить в CLAUDE.md напоминание |

### Tests & Coverage

- Тестов: 10 файлов, CI статус недоступен (GOOGLE_AI_API_KEY не в GitHub Secrets — стратег зафиксировал)
- Ключевой пробел: scorer.ts после overhaul (sqrt curve, low-skills penalty) — нужны тесты новых edge cases
- `resume-import.validateResumeData` — тесты есть, но `extractPdfText` не тестирован

### Monetization & UX

- Free-to-Pro flow: технически реализован (Telegram Stars, trial 100⭐, weekly 200⭐, monthly 700⭐)
- 2 пользователя в DB, 0 платёжных транзакций — конверсия не измерима при текущей аудитории
- Стратег прав: P0 — пост в Telegram чатах, Habr-статья. Любой рост кода без этого бессмысленен
- Digest фильтры работают (исправлено в v0.24.x) — UX OK

### Backlog

- Open: 8, Done: 55 — самый чистый бэклог из всех проектов
- Все 8 открытых — долгосрочные фичи (500+ юзеров): FTS5, LinkedIn, interview prep
- Блокер: GOOGLE_AI_API_KEY в GitHub Secrets — CI стратега сломан

---

## Vedic Turkiye

### Code Health

| Severity | Issue | File:line | Fix |
|---|---|---|---|
| critical | HTTP 404 третью неделю подряд — сайт мёртв в продакшне | Vercel dashboard | Проверить DATABASE_URL в Vercel env vars, смотреть build logs |
| critical | VedicAstroAPI trial ~28 марта — 5 дней, 498 вызовов осталось | VT-703 | Оплатить $18/мес СЕГОДНЯ |
| high | 3 high npm vulnerabilities не закрыты | `package.json` | `npm audit` — определить пакеты, обновить или override |
| high | `rateLimit.ts` in-memory хранилище — на Vercel serverless каждая invocation = новый instance, rate limit не работает | `lib/utils/rateLimit.ts:3` | Использовать Vercel KV или Upstash Redis, или принять ограничение |
| high | `BirthDataForm.tsx` 792 строк — форма + place autocomplete + summary + validation смешаны | `BirthDataForm.tsx:53,737` | Вынести `SummaryRow` и place autocomplete в отдельные компоненты |
| medium | `ChartResult.tsx` 658 строк — ShareCard (523), SummaryCard (494), formatInterpretation (13) | `ChartResult.tsx:494,523` | Вынести ShareCard, SummaryCard |
| medium | `calculate.ts` 572 строки — parsePlanets (345), parseAscendant (409), estimateDashaDates (449) — 4 парсера в одном файле | `calculate.ts:345` | Вынести parse-функции в `calculate-parsers.ts` |
| medium | `blog/[slug]/page.tsx` — heading detection: `paragraph.length < 80 && !paragraph.endsWith(".")` — ложные срабатывания | `blog/[slug]/page.tsx:111` | Явно маркировать заголовки в Article type |
| medium | `app/api/reports/generate/route.ts` — если ADMIN_SECRET не задан: 500 вместо понятной ошибки | `route.ts:27-31` | Сообщение "ADMIN_SECRET not configured" уже есть — OK |
| low | `payment/success/page.tsx` — кнопка "Скачать отчёт" ведёт на `href="#"` — нефункциональна | `page.tsx:128` | Заглушка до реализации VT-702 — добавить `disabled` стиль |
| low | `sitemap.ts` BASE = `"https://vedic-turkiye.vercel.app"` — не кастомный домен | `sitemap.ts:4` | Обновить после VT-001 (выбор домена) |
| low | `middleware.ts` matcher `["/"]` — locale detection только на root, остальные страницы без авто-детекции | `middleware.ts:63` | Intentional (TR/EN дублируют URL), но стоит задокументировать |

### Calculator & AI

- Калькулятор в BETA — верификация VT-003 (Duygu тест 10 карт) не выполнена
- VedAstro API работает (200 OK), расчёты совпадают с Prokerala/VedicAstroAPI (проверено 19.03)
- AI интерпретации: ANTHROPIC_API_KEY нужен для premium отчётов — неизвестно, настроен ли в Vercel
- `generatePremiumReportStream` реализован (VT-700 закрыт) — хорошо
- 57 тестов на chart validation добавлено в последнем спринте — прогресс

### Payments & SEO

- Shopier: не подключён — ждёт VT-002 (Duygu регистрирует аккаунт)
- `createShopierPayment` реализован, `buyer_phone: "05000000000"` — заглушка, могут быть проблемы с валидацией
- SEO: robots.txt и sitemap добавлены (a71aad2) — хорошо
- hreflang теги (VT-822): не реализованы — потеря SEO для EN страниц
- JSON-LD (VT-821): не реализован

### Blockers

- VT-001: Duygu выбирает домен
- VT-002: Duygu регистрирует Shopier (блокирует VT-701, VT-702)
- VT-003: Duygu тестирует 10+ карт (блокирует VT-207)
- VT-703: Оплатить VedicAstroAPI до 28 марта — **5 дней**

---

## Portfolio

### Code & Design

| Severity | Issue | File:line | Fix |
|---|---|---|---|
| high | GoatCounter `diyordesign.goatcounter.com` — аккаунт не зарегистрирован 3+ недели | `index.html:27` | Зарегистрировать на goatcounter.com, занимает 2 минуты |
| medium | `script.js` 285 строк — нет инициализации mobile nav burger | `script.js` | Проверить работу бургера на 375px — если сломан, добавить toggle |
| medium | `vedic-v2.html` zombie file — стратег зафиксировал (ZOMBIE-01) | Portfolio root | `git rm vedic-v2.html` |
| low | `styles.css` 1657 строк — монолит, но для vanilla CSS без препроцессора это нормально | — | Не критично |
| low | Год "2026" в кейсе Qlean неверный — QLEAN-META открыта | `projects/qlean.html` | Исправить на 2018–2021 |

### Content & SEO

- Распределение: Черновики готовы 3+ недели — Telegram, LinkedIn, HH — ни один не опубликован
- OG-image для `about.html` отсутствует (SEO-05 открыта)
- Sitemap без `lastmod` (SEO-04 открыта)
- Скрины Hunter, SAMI — не добавлены (CONTENT-03, CONTENT-04)
- Vedic кейс: дизайн обновился, скрины старые (SYNC-02)
- Доступность: ARIA-роли на табах (A11Y-07), `inline onclick` (A11Y-08), бургер ARIA (A11Y-09) — открыты

---

## Dependencies & Security

| Project | Vulnerabilities | Outdated | Action needed |
|---|---|---|---|
| Hunter | 0 | @types/node 20→25 | Обновить @types/node |
| SAMI | 0 | @anthropic-ai/sdk 0.78→0.80, @types/node | Обновить SDK |
| Vedic Turkey | **3 HIGH** | Prisma 6→7, eslint 9→10, react 19.2.3→19.2.4, next-auth beta→4.24.13 | `npm audit` немедленно, обновить react |
| Portfolio | — | — | Нет npm |

Vedic: `next-auth 5.0.0-beta.30` vs latest `4.24.13` — обратная совместимость нарушена при даунгрейде, не обновлять на 4.x. Ждать stable v5.

---

## Cross-Project

- **Shared логгер**: SAMI `createLogger` с correlation IDs — элегантно. Hunter `logger` проще (без correlation). Если Hunter масштабируется, стоит взять паттерн SAMI.
- **Zod config validation**: оба бота используют одинаковый паттерн — хорошо
- **Rate limiting**: Vedic использует in-memory (не работает на serverless), боты используют SQLite (работает для single-instance Railway)
- **AI fallback chain**: Hunter — Gemini→Groq, Vedic — Claude (required). Разные модели — это правильно по назначению
- **Дистрибуция**: у всех 4 проектов нет внешнего движения. Это кросс-проектный паттерн, не технический

---

## Action Items

1. **Vedic** — critical — Исправить 404: открыть Vercel dashboard → Deployments → найти failed build → проверить DATABASE_URL в Environment Variables → передеплоить — немедленно
2. **Vedic** — critical — Оплатить VedicAstroAPI $18/мес до 28 марта (5 дней, 498 вызовов осталось) — VT-703
3. **Hunter** — critical — Добавить `GOOGLE_AI_API_KEY` в GitHub → Settings → Secrets → Actions — 5 минут, фиксирует CI стратега
4. **Vedic** — high — `npm audit fix` — выявить 3 high vulnerabilities, обновить react 19.2.3→19.2.4
5. **Hunter** — high — Опубликовать пост в 3 Telegram чата с разработчиками/дизайнерами — стратег прав: один пост = 20-50 юзеров
6. **Portfolio** — high — Опубликовать черновики (Telegram @diyoriko, LinkedIn Featured, HH описание) — DIST-NOW
7. **Portfolio** — high — Зарегистрировать аккаунт на goatcounter.com (diyordesign) — 2 минуты, 3 недели потеряны
8. **Portfolio** — medium — `git rm vedic-v2.html && git commit` — ZOMBIE-01
9. **SAMI** — medium — SM-1010: вынести videos, posts, members, challenges из `db.ts` (остаток ~1800L после db-approval.ts) — фаза 2-5
10. **SAMI** — medium — Вынести `sendMyWorkouts`, `sendChallengeView`, `sendFilterResults` из `bot-menu.ts:1745,1875,1950` в отдельный файл
11. **Hunter** — medium — Проверить `resume-import.ts:22-24`: `new PDFParse({ data: buffer })` на совместимость с pdf-parse v2 — риск молчащего бага
12. **Vedic** — medium — `rateLimit.ts`: задокументировать ограничение (in-memory = не работает на serverless) — добавить TODO комментарий или перейти на Upstash Redis — `lib/utils/rateLimit.ts:3`
13. **Portfolio** — medium — Проверить mobile nav burger на 375px (JS toggle), если сломан — добавить в `script.js`
14. **SAMI** — medium — Написать оставшимся 9 знакомым (SM-1000: 1/10 за 2 недели) — единственный рычаг роста при 16 подписчиках
15. **Vedic** — low — `payment/success/page.tsx:128` — кнопка "Raporu İndir" (`href="#"`) — добавить `disabled` стиль и tooltip "Yakında" до реализации VT-702
