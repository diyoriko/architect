Теперь у меня достаточно данных. Пишу review.

---

# Mega Review — 2026-03-17

## Summary

SAMI и Hunter технически стабильны: тесты идут, деплой работает, основные фичи закрыты. Главная боль этой недели — три монолита по 1500+ строк (bot-menu.ts, bot.ts, db.ts) и критический баг в SAMI: данные от Mac-стратега не записываются в БД. Hunter несёт устаревший Claude model ID, который скоро сломает A/B тест обложек. Бэклоги приоритизированы адекватно, но есть мёртвый код в approval-флоу SAMI.

---

## SAMI

### Code Health

| Severity | Issue | File:line | Fix |
|---|---|---|---|
| **critical** | `payload.packet` валидируется как `string`, но `savePacketFromExternal` ожидает `Partial<StrategistPacket>`. Spread строки в объект даёт мусор → Mac-стратег пишет DEFAULT_PACKET вместо реальных данных | `index.ts:~530` | `JSON.parse(payload.packet)` перед вызовом; `payload.report` → `{ full_report: payload.report }` |
| **high** | `challengeContextMap` — in-memory Map теряется при рестарте. Если бот упал между approval и кликом "Выбрать", challenge-контекст потерян | `approval.ts:26` | Сохранять `challengeId/dayNumber` в `approval_sessions` как JSON-поле |
| **high** | `reject` callback зарегистрирован для pattern `approve\|reject`, но кнопки "Отклонить" в клавиатуре нет — dead handler | `approval.ts:~280` | Убрать `reject` из regex или добавить кнопку |
| **medium** | `sendApprovalCard` использует `Function` тип — теряет типобезопасность | `approval.ts:88` | Заменить на конкретные сигнатуры из grammy API |
| **medium** | `CATEGORY_EMOJI` и `CATEGORY_EMOJI_MAP` — два словаря с **разными** значениями (`mobility: '🐍'` vs `'🤸'`). Посты и челленджи показывают разные эмодзи | `shared.ts:~43` vs `~213` | Один источник правды, удалить `CATEGORY_EMOJI_MAP` |
| **medium** | `require('child_process')` внутри `findYtDlp()` вместо top-level import | `downloader.ts:78` | Использовать импорт из верхней части файла |
| **low** | Импорты разбиты: `const log = createLogger()` объявлен между двумя группами импортов | `analytics.ts:8` | Переместить все импорты наверх |
| **low** | `escapeMarkdown` помечен `@deprecated` — проверить что нигде не используется и удалить | `shared.ts:~305` | `grep escapeMarkdown` → удалить |

**Размер файлов (> 500 строк):**
- `bot-menu.ts` — 1565 строк: минимум 3 логических модуля (клавиатуры, UGC-flow, фильтры)
- `db.ts` — 2122 строки: разбить на `db/videos.ts`, `db/challenges.ts`, `db/analytics.ts`, `db/members.ts`
- `moderation.ts` — 881 строка

### UX & Content

- Approval-флоу для 7 категорий даёт 7+ сообщений в DM за раз — тяжело читать. Рассмотреть сводное меню "неделя" как единственную точку входа
- `/search` ищет на **завтра**, `/post` пробует оба дня — администратор вынужден знать это поведение наизусть
- Текст капчи и welcome не проверял (bot-menu.ts не показан полностью), но исходя из бэклога — задача уже в P1

### Backlog

- Open: P0×2, P1×4, P2×6. Баланс: 30% security, 40% UX, 30% tech debt — **нормально для стадии**
- P0 (Fisher-Yates, admin ID) — правильный приоритет, должны закрыться в этом спринте
- Устаревших задач нет: всё было добавлено в марте

---

## Hunter

### Code Health

| Severity | Issue | File:line | Fix |
|---|---|---|---|
| **high** | Устаревший model ID `'claude-sonnet-4-20250514'` — модель может быть депрекирована; A/B тест 50% пользователей использует стареющий sonnet | `cover-letter.ts:140` | `'claude-sonnet-4-6'` |
| **medium** | `employerIndustry` параметр в `scoreDomain()` никогда не передаётся из `scoreVacancy` — dead code, доменная логика не работает через industry field | `scorer.ts:~273` | Передать `v.employerIndustry` из вакансии или удалить параметр |
| **medium** | `sendReengagementPush` (каждые 90 дней) + `processReminders` с типом `reengagement` — два механизма могут слать одному пользователю два сообщения в один день | `scheduler.ts:~260` | Объединить в один flow или добавить флаг `reengagement_sent_at` |
| **medium** | Hardcoded currency rates `USD→RUB × 90`, `EUR→RUB × 95` — стали неточными, влияют на salary score | `scorer.ts:~180` | Кешировать курс из ЦБ РФ раз в день |
| **low** | `version: '0.21.0'` в config хардкодом — рассинхронизируется с package.json | `config.ts:16` | `JSON.parse(fs.readFileSync('package.json')).version` |
| **low** | `GetmatchScraper` принимает `queries` в конструкторе, но не использует (API не поддерживает поиск). Параметр называется `_queries` — сбивает с толку | `scrapers/getmatch.ts:45` | Убрать параметр из конструктора, упростить интерфейс |

**Размер файлов:**
- `bot.ts` — 1744 строки: критично, разбить на `handlers/digest.ts`, `handlers/payments.ts`, `handlers/onboarding.ts`
- `db.ts` — 1208 строк
- `onboarding.ts` — 951 строка

### Monetization & UX

- Freemium flow: Free получает 2 карточки + тизер → корректно
- `creditsPerLetter: 5` с комментарием `1 restyle = 5 credits` — нейминг путает. Лучше `creditsPerRestyle` или просто убрать промежуточный слой
- Paywall UX не смотрел в деталях (bot.ts не показан полностью), но по бэклогу Sprint 8 закрыл основные кейсы

### Backlog

- Open: P1×5, P2×4, P3×6. Баланс: 60% stability, 25% UX, 15% future — **правильно после Sprint 8**
- P1 (silent catch logging, timeout guards) — должны войти в следующий спринт
- P3 (LinkedIn, EN, mini apps) — адекватно заморожены

---

## Cross-Project

**Дублирование:**
- Оба проекта: raw HTTP server с идентичной логикой `/backup` (WAL checkpoint + stream), `/proposal`, `/proposals` — можно вынести в shared util, но при 2 проектах не критично
- Auth fallback `API_KEY ?? BOT_TOKEN` повторяется 5+ раз в каждом `index.ts` — хотя бы вынести в функцию `getAuthToken()`
- Оба используют одинаковые паттерны SQLite migrations (addColumn, runMigrations)

**Несоответствия:**
- SAMI logger имеет `correlationId` + `withCorrelation` → хорошая практика. Hunter logger — нет. При переходе к multi-agent debugging это будет ощущаться
- SAMI использует MarkdownV2 + `escV2`, Hunter — HTML + `escapeHtml`. Разные parse_mode в одной экосистеме — нормально, но надо помнить при шаринге утилит

---

## Action Items

1. **SAMI** — Исправить критический баг: `JSON.parse(payload.packet)` в `/packet` handler — `index.ts:~530`
2. **SAMI** — Сохранить `challengeContext` в БД вместо in-memory Map — `approval.ts:26`
3. **SAMI** — Удалить мёртвый `reject` из callback pattern или добавить кнопку — `approval.ts:~280`
4. **SAMI** — Удалить `CATEGORY_EMOJI_MAP`, унифицировать с `CATEGORY_EMOJI` — `shared.ts:~213`
5. **Hunter** — Обновить model ID до `claude-sonnet-4-6` — `cover-letter.ts:140`
6. **Hunter** — Передать `employerIndustry` в `scoreVacancy` или удалить параметр — `scorer.ts:~273`
7. **Hunter** — Дедуплицировать reengagement: убрать один из двух механизмов — `scheduler.ts:~260`
